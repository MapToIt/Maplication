import { AddTablesDirective } from './add-tables.directive';

describe('AddTablesDirective', () => {
  it('should create an instance', () => {
    const directive = new AddTablesDirective();
    expect(directive).toBeTruthy();
  });
});
